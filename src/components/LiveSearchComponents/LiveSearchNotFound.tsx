import { ListGroup } from "react-bootstrap"

const LiveSearchNotFound = () => {
  return (
    <ListGroup>
      <ListGroup.Item className="d-flex justify-content-center align-items-center">
        Not found
      </ListGroup.Item>
    </ListGroup>
  );
};

export default LiveSearchNotFound